import tkinter as tk

def hello():
    print("hello there")
otk = tk.Tk()
otk.geometry("100x150+400+400") #윈도우 위젯 객체 크기설정(geometry 메소드 이용)
obtn = tk.Button(otk,text='click',command = hello)
obtn.pack()
otk.mainloop()
#문자열 출력

na = 10
nb = 20
nc =na + nb

print("na값",na,"더하기",nb,"의 결과값은",na+nb,"이다.")

print("na값 %d 더하기 %d 의 결과값은 %d 이다."%(na,nb,na+nb))

print("na값 {} 더하기 {} 의 결과값은 {} 이다.".format(na,nb,na+nb))

print(f"na값 {na} 더하기 {nb} 의 결괏값은 {na+nb} 이다.")

#인덱싱_음수

r =[1,2,3,4,5,6,7,8,9]

print(r[-2])

#슬라이싱_음수

print(r[-7:0])
print(r[0:-7])
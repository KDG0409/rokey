#지역 변수로 초기화(정의)

b = 0
print("b의 값은",b) #0
b = 1
print("b의 값은",b) #1

def scope_test():
    a = 0
    a += 3
    print("scope_test() 함수 안의 a 값은",a)
    return a

a = 0
print("scope_test() 함수 안의 a 값은",a) #0
a = scope_test() #3
print("scope_test() 함수 안의 a 값은",a) #3
#1. statsmodels
# import statsmodels.api as sm
# data = sm.datasets.get_rdataset("rtcar").data
# X=[1,2,3,4]
# y=[1,2,3,4]
# X=sm.add_constant(X)
# ans=sm.OLS(y,X).fit()
# ans.summary()

#2. sklearn
# import pandas as pd
# from sklearn import svm,matrics
# from sklearn.datasets import load_iris
# from sklearn.model_selection import train_test_split
# iris = load_iris
# df=pd.DataFrame(iris,columns=data.features_names)
# df['target'] = df.target
# target_names={0:iris.target_names[0],1:iris.target_names[1],0:iris.target_names[1]}
# df['target']=df['target'].map(target_names)
# dataA=df['','','']
# dataB=df['target']
# train_d,test_d,train_l,test_l=train_test_split(dataA,dataB)

# dataC=svm.SVC()
# dataD=dataC.fit(train_d,train_l)
# dataE=dataD.predict(test_d)
# dataF=matrics.accuracy_score(dataE,test_l)
# print(dataF)

#3. scpiy
from scipy.linalg import solve
#solve(x,y)
from scipy.optimize import minimize
#minimize(def,x0)
from scipy.optimize import root
#root(def,x0)
from scipy.stats import describe
#describe(list)
from scipy.sparse import csr_matrix
#csr_matrix(matrix)

import tkinter as tk

oroot = tk.Tk()

ostring = tk.StringVar() #ostring을 문자열 형태로 자동 결정(동적할당)
oentry = tk.Entry(oroot,textvariable=ostring) #입력값을 ostring에 저장
oentry.pack()
olabel = tk.Label(oroot,textvariable=ostring)
olabel.pack()

oroot.mainloop()
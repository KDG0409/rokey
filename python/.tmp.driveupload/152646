#statsmodels
import statsmodels.api as sm
import matplotlib.pyplot as plt
X = [1,2,3,4,5]
y = [2,4,6,8,10]
# X = sm.add_constant(X)
# data = sm.OLS(y,X).fit()
# print(data.summary())

plt.title('Linear Regression')
plt.xlabel('X')
plt.ylabel('y')
plt.plot(X,y,'r')
plt.scatter(X,y)
plt.show()